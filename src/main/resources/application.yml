
spring:
  batch:
    job:
      names:${job.name:NONE}

---
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/study?serverTimezone=Asia/Seoul&rewriteBatchedStatements=true
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
---




#spring:
#  datasource:
#    driver-class-name: org.h2.Driver
#  sql:
#    init:
#      data-locations: classpath:person.sql

---
  spring:
    batch:
      jdbc:

       initialize-schema: always